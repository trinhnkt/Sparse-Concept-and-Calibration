\section{Diagnostic Protocol}
\label{sec:protocol}

\subsection{Data Schema}
Each dataset is normalized into a common interaction schema:
\begin{equation}
(u_t, i_t, c_t, r_t, \tau_t),
\end{equation}
where $u_t$ is the learner, $i_t$ is the item, $c_t$ is the KC, $r_t\in\{0,1\}$ is correctness, and $\tau_t$ is the timestamp. Multi-skill items should be handled according to the preprocessing convention of the chosen benchmark implementation and documented in the artifact.

\subsection{Split Modes}
We evaluate three split modes. The learner-based split assigns learners to train/validation/test partitions without learner overlap. The temporal split trains on earlier interactions and evaluates on later interactions. The limited cold-start concept split evaluates KCs with limited training-fold frequency. These modes answer different diagnostic questions and should not be conflated.

\subsection{KC-frequency Stratification}
For each training fold, we compute the training frequency of a KC $c$ as
\begin{equation}
\mathrm{freq}_{\mathrm{train}}(c)=\sum_{(u,i,c',r,\tau)\in D_{\mathrm{train}}}\mathbb{I}[c'=c].
\end{equation}
KCs are assigned to four pre-registered strata:
\begin{align}
\text{Very Sparse:} &\quad \mathrm{freq}_{\mathrm{train}}(c) < 20,\\
\text{Sparse:} &\quad 20 \le \mathrm{freq}_{\mathrm{train}}(c) < 100,\\
\text{Medium:} &\quad 100 \le \mathrm{freq}_{\mathrm{train}}(c) < 500,\\
\text{Dense:} &\quad \mathrm{freq}_{\mathrm{train}}(c) \ge 500.
\end{align}
The assignment must use only training-fold counts. Validation and test frequencies may be logged for auditing but must not determine the bucket.

\subsection{Calibration Metrics}
Given predictions $p_i$ and labels $y_i$, ECE with $M=15$ bins is
\begin{equation}
\ece = \sum_{m=1}^{M}\frac{|B_m|}{N}\left|\mathrm{acc}(B_m)-\mathrm{conf}(B_m)\right|,
\end{equation}
where $\mathrm{acc}(B_m)$ is empirical correctness and $\mathrm{conf}(B_m)$ is average predicted confidence in bin $B_m$.

The Brier score is
\begin{equation}
\brier = \frac{1}{N}\sum_{i=1}^{N}(p_i-y_i)^2.
\end{equation}
We use the standard decomposition
\begin{equation}
\brier = \unc - \res + \rel,
\end{equation}
where $\rel$ captures reliability error, $\res$ captures resolution, and $\unc$ captures intrinsic uncertainty. Calibration is reported both overall and per KC stratum.

\subsection{Seven-channel Leakage Audit}
Table~\ref{tab:leakage} summarizes the leakage audit. The audit is not a model component; it is an evaluation hygiene requirement.

\input{tables/table_leakage_audit}

\subsection{Diagnostic Pipeline}
Figure~\ref{fig:pipeline} illustrates the pipeline.

\begin{figure}[t]
\centering
\fbox{\begin{minipage}{0.95\linewidth}
\small
\textbf{Pipeline.} Raw KT logs $\rightarrow$ schema normalization $\rightarrow$ learner/temporal/cold-start splits $\rightarrow$ train-only KC-frequency buckets $\rightarrow$ baseline training $\rightarrow$ prediction export $\rightarrow$ AUC/ACC/NLL/RMSE $\rightarrow$ ECE/Brier/reliability diagrams per bucket $\rightarrow$ leakage audit $\rightarrow$ reproducible report.
\end{minipage}}
\caption{Overview of the reproducible sparse-concept and calibration diagnostic pipeline. Replace this placeholder with a vector diagram before submission.}
\label{fig:pipeline}
\end{figure}
